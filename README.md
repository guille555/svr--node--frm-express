# svr--node--frm-express
A simple example of express framework. The goal is serve static files.
The server is listening in port 3000.
To run the project execute "npm run start".

Technologies used:

  * Node JS
  * Express
  * VSCode
